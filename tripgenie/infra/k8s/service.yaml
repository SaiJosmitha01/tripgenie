apiVersion: v1
kind: Service
metadata:
  name: tripgenie-backend-placeholder
  namespace: tripgenie
spec:
  selector:
    app: tripgenie-backend-placeholder
  ports:
    - name: http
      port: 80
      targetPort: 8080
